<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.myweb.snsboard.mapper.ISnsBoardMapper">
<!-- IFreeBoardMapper.java에서 namespace 가져옴 -->
<!-- 테스트 환경 구축!  -->
<!-- POM.XML에 MAVEN 라이브러리 추가 -->
<!-- root-context 에 mybatis-spring:scan 을 넣어야 빈등록이 된다-->

	<insert id="insert">
		INSERT INTO snsboard
		(bno, writer, upload_path, file_loca, file_name, file_real_name, content)
		VALUES
		(snsboard_seq.NEXTVAL, #{writer}, #{uploadPath}, 
		#{fileLoca}, #{fileName}, #{fileRealName}, #{content})
	</insert>
	
	<select id="getList" resultType="sns">
	SELECT * FROM
		(
		SELECT ROWNUM AS rn, tbl.*
			FROM
			(
			SELECT * FROM snsboard
			ORDER BY bno DESC
			) tbl
		)
	<![CDATA[
	WHERE rn > #{pageStart} 
	AND rn <= #{pageEnd}
	]]>
	</select>
	
	<select id="getDetail" resultType="sns">
		SELECT content, writer, file_loca, file_name, reg_date
		FROM snsboard
		WHERE bno = #{bno}

	</select>
	



	


</mapper>